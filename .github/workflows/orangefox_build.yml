name: Build OrangeFox Recovery for OnePlus 11R (udon)

on:
  workflow_dispatch:
    inputs:
      FOX_BRANCH:
        description: 'OrangeFox Branch (12.1 or 14.1)'
        required: true
        default: '12.1'
        type: choice
        options:
          - '12.1'
          - '14.1'
      BUILD_TARGET:
        description: 'Build Target'
        required: true
        default: 'recoveryimage'
        type: choice
        options:
          - 'recoveryimage'
          - 'bootimage'

jobs:
  build:
    name: Build OrangeFox (${{ inputs.FOX_BRANCH || '12.1' }})
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Device Tree
        uses: actions/checkout@v4
        with:
          path: device/oneplus/udon

      - name: Free Disk Space & Setup Swap
        run: |
          echo "=== Freeing Disk Space ==="
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/boost /usr/share/swift || true
          sudo docker image prune -a --force || true
          
          echo "=== Setting up 16GB Swapfile ==="
          sudo swapoff -a || true
          sudo rm -f /swapfile /mnt/swapfile || true
          sudo fallocate -l 16G /mnt/swapfile || sudo dd if=/dev/zero of=/mnt/swapfile bs=1M count=16384
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile
          free -h
          df -h

      - name: Install Build Dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y git-core gnupg flex bison build-essential zip curl zlib1g-dev \
            gcc-multilib g++-multilib libc6-dev-i386 libncurses5 lib32ncurses5-dev x11proto-core-dev \
            libx11-dev lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig python3 python-is-python3 libssl-dev libtinfo5 rsync bc
          sudo curl --create-dirs -L -o /usr/local/bin/repo -O -L https://storage.googleapis.com/git-repo-downloads/repo
          sudo chmod a+rx /usr/local/bin/repo

      - name: Sync OrangeFox Sources with Official Sync Tool
        run: |
          git config --global user.name "The LineageOS Project"
          git config --global user.email "dev@lineageos.org"
          git clone https://gitlab.com/OrangeFox/sync.git ~/OrangeFox_sync
          cd ~/OrangeFox_sync
          ./orangefox_sync.sh --branch ${{ inputs.FOX_BRANCH || '12.1' }} --path /home/runner/fox

      - name: Integrate OnePlus 11R Device Tree
        run: |
          rm -rf /home/runner/fox/device/oneplus/udon
          mkdir -p /home/runner/fox/device/oneplus
          cp -r $GITHUB_WORKSPACE/device/oneplus/udon /home/runner/fox/device/oneplus/

      - name: Build OrangeFox Recovery
        shell: bash
        run: |
          cd /home/runner/fox
          set +e
          export ALLOW_MISSING_DEPENDENCIES=true
          export FOX_USE_TWRP_RECOVERY_IMAGE_BUILDER=1
          export FOX_VIRTUAL_AB_DEVICE=1
          export TARGET_DEVICE=udon
          export LC_ALL="C"
          
          echo "=== Patching rsync in build system for Virtual A/B vendor symlinks ==="
          sed -i 's/rsync -a --exclude=sdcard/rsync -a -K --keep-dirlinks --exclude=vendor --exclude=sdcard/g' build/make/core/Makefile || true

          echo "=== Sourcing envsetup.sh ==="
          source build/envsetup.sh
          
          echo "=== Running lunch ==="
          lunch fox_udon-eng
          LUNCH_STATUS=$?
          if [ $LUNCH_STATUS -ne 0 ]; then
            echo "fox_udon-eng failed, trying fox_udon-userdebug..."
            lunch fox_udon-userdebug
            LUNCH_STATUS=$?
          fi
          if [ $LUNCH_STATUS -ne 0 ]; then
            echo "fox_udon-userdebug failed, trying omni_udon-eng..."
            lunch omni_udon-eng
            LUNCH_STATUS=$?
          fi
          if [ $LUNCH_STATUS -ne 0 ]; then
            echo "omni_udon-eng failed, trying twrp_udon-eng..."
            lunch twrp_udon-eng
            LUNCH_STATUS=$?
          fi

          echo "=== Starting Compilation ==="
          set -e
          mka ${{ inputs.BUILD_TARGET || 'recoveryimage' }} -j$(nproc --all)

      - name: Upload Recovery Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: OrangeFox-OnePlus11R-${{ github.sha }}
          path: |
            /home/runner/fox/out/target/product/udon/OrangeFox*.zip
            /home/runner/fox/out/target/product/udon/recovery.img
            /home/runner/fox/out/target/product/udon/boot.img
            /home/runner/fox/out/target/product/udon/vendor_boot.img
          if-no-files-found: warn
